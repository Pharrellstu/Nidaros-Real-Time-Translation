global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - prometheus-alerts.yml

scrape_configs:
  - job_name: 'wowza-captions'
    static_configs:
      - targets:
          - wse:9101

